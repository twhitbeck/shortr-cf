{
  "name": "shortr-worker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild --bundle --platform=neutral --outdir=dist --out-extension:.js=.mjs src/index.ts",
    "clean": "rm -rf dist",
    "dev": "miniflare --watch --modules --debug",
    "test": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.1.1",
    "esbuild": "^0.13.13",
    "miniflare": "^1.4.1",
    "prettier": "^2.4.1",
    "typescript": "^4.5.1-rc"
  }
}
